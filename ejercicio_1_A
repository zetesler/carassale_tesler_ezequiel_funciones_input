# 1A - Realizar una función para pedir un número ENTERO
#______________________________________________________

def get_int(mensaje: str, mensaje_error: str, minimo: int, maximo: int, reintentos: int) -> int|None:

    """
    Función que solicita al usuario el ingreso de un número ENTERO y lo valida.
    mensaje: Mensaje que se va a imprimir antes de pedirle al usuario el dato por consola.
    mensaje_error: Mensaje de error en el caso de que el dato ingresado sea invalido.
    mínimo: Valor mínimo admitido (inclusive)
    máximo: Valor máximo admitido (inclusive)
    reintentos: Cantidad de veces que se volverá a pedir el dato en caso de error.
    return: Número ingresado o None

    """
    contador_intentos = 0

    while contador_intentos < reintentos:
        numero_usuario = int(input(mensaje))
        if numero_usuario >= minimo and numero_usuario <= maximo:
            print(f"USTED HA INGRESADO EL NÚMERO {numero_usuario}.")
            return numero_usuario
        else:
            print(mensaje_error)
            contador_intentos += 1
            
    print("USTED HA ALCANZADO EL LÍMITE DE INTENTOS")
    return None


#****************************************************************************************************************

# EXPLICACIÓN LÍNEA POR LÍNEA:

"""

- LÍNEA 4: Definición de la función y especificación de parámetros + tipo de parámetro + retorno.

- LÍNEA 16: Contador de intentos, inicializado en cero.

- LÍNEA 18: Inicio bucle While, que se activa solamente si el valor contador de intentos es menor.
al parámetro "reintentos" cuyo valor es especificado al llamar a la función.

- LÍNEA 19: El programa pide al usuario el ingreso de un número decimal entre 1 y 200 y lo guarda
en la variable "numero_usuario".

- LÍNEAS 20/21: Si el número ingresado está entre los valores de los parámetros MÍNIMO y MÁXIMO
especificados al llamar a la función, imprime el mensaje con el número ingresado.

- LÍNEA 22: Retorna el número ingresado por el usuario.

- LÍNEAS 23/24: Si el número ingresado no es válido, imprime el mensaje de error.

- LÍNEA 25: Suma un intento al contador de intentos.

- LÍNEA 27: Anuncia que ya se terminaron los intentos una vez que se alcanzó el límite establecido
al llamar a la función.

- LÍNEA 28: Retorna None si el usuario ingresó erróneamente en todos los intentos.

- LÍNEAS 30/36: Llamada a la función + determinación de los valores de los parámetros.

"""
